{
  const fruits: string[] = ['사과', '바나나'];
  const score: Array<number> = [1, 2];

  function printArray(fruits: readonly string[]){}

  // tuple -> 무조건 남용하는 것은 지양 : interface, type alias, class로 처리 가능하면 그렇게 처리하는게 좋음
  let stu1 : [string, number]


}
